{
  "name": "livya-smart-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["client", "admin"],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "dev:admin": "npm --workspace admin run dev",
    "build": "npm run build --workspaces"
  }
}